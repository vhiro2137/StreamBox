# StreamBox 在线网络播放器 UI/UX Design 设计文档

| 项目 | 内容 |
|---|---|
| 产品名称 | StreamBox（暂定） |
| 文档类型 | UI/UX 视觉与交互设计规范 |
| 文档版本 | V1.0 |
| 对应 PRD | `PRD_OnlineMediaPlayer.md` V1.0 |
| 目标平台 | Windows 桌面端，预留 Linux/macOS 适配 |
| 实现框架 | Qt Widgets / C++ |
| 基准画布 | 1440 × 900 px |
| 设计主题 | 深色、专业、沉浸、简洁 |
| 日期 | 2026-08-15 |

## 1. 文档目的

本文档用于指导 Figma、MasterGo、即时设计及 AI UI 设计工具生成可直接交付开发的 StreamBox V1.0 界面。设计稿必须覆盖主界面、空状态、音频播放、视频播放、网络加载、缓冲、暂停、错误、播放结束、URL 对话框、倍速菜单、播放模式、播放列表及全屏控制等状态。

本文档只定义 V1.0 界面，不得擅自加入登录、会员、推荐、搜索、下载、弹幕、评论、直播频道、网页解析或 DRM 绕过相关入口。

## 2. 设计目标

1. 播放内容是视觉中心，播放器控件不能喧宾夺主。
2. 高频操作应在主界面一次点击完成：播放、暂停、进度、上下首、音量、倍速和全屏。
3. 本地媒体与网络媒体共享一致的播放体验，只在来源、加载和错误状态上做必要区分。
4. 每个状态都必须可感知，尤其是正在连接、缓冲、不可 Seek、重试和错误。
5. 设计应能由 Qt Widgets + QSS 准确实现，避免大量模糊、复杂玻璃材质和难以跨平台还原的特效。

## 3. 视觉定位

### 3.1 关键词

专业播放器、现代桌面软件、深色沉浸、低干扰、清晰层级、细腻但克制。

### 3.2 参考气质

- 采用媒体创作工具常见的深灰工作区，而非纯黑大面积界面。
- 视频区使用纯黑，保证视频画面和黑边自然融合。
- 强调色仅用于当前进度、主操作、当前播放项和焦点。
- 不使用霓虹渐变、大面积高饱和色、拟物按钮或娱乐化卡片瀑布流。

### 3.3 品牌概念

- 产品名：StreamBox。
- Logo 建议：圆角方形容器内组合“播放三角形 + 流动波纹”。
- Logo 图形尺寸 24 × 24 px，应用图标输出 16、24、32、48、64、128、256 px。
- 首版设计稿可使用简化图形占位，禁止使用第三方品牌 Logo。

## 4. 设计基础系统

### 4.1 色彩 Token

| Token | 色值 | 用途 |
|---|---|---|
| `color.window` | `#111318` | 应用窗口底色 |
| `color.surface.1` | `#171A21` | 顶栏、底部控制区 |
| `color.surface.2` | `#1D212A` | 播放列表、菜单、对话框 |
| `color.surface.3` | `#252A35` | 悬停、选中前景、输入框 |
| `color.video` | `#000000` | 视频画布及信箱黑边 |
| `color.border` | `#303642` | 分隔线、描边 |
| `color.border.focus` | `#6C8CFF` | 键盘焦点、输入焦点 |
| `color.text.primary` | `#F3F5F7` | 主文本 |
| `color.text.secondary` | `#AEB6C4` | 次级文本 |
| `color.text.tertiary` | `#737D8E` | 辅助信息、禁用前文本 |
| `color.text.disabled` | `#525A68` | 禁用文本和图标 |
| `color.accent` | `#6C8CFF` | 主操作、当前进度、选中态 |
| `color.accent.hover` | `#7D9AFF` | 强调控件 Hover |
| `color.accent.pressed` | `#5878E8` | 强调控件 Pressed |
| `color.accent.subtle` | `#283354` | 当前列表项背景 |
| `color.success` | `#41C98E` | 成功/恢复连接 |
| `color.warning` | `#F2B84B` | 缓冲、重试 |
| `color.error` | `#F06A73` | 错误和危险操作 |
| `color.overlay` | `rgba(0,0,0,0.62)` | 视频浮层 |

色彩要求：正文与背景对比度尽量达到 WCAG AA；任何状态不能只靠颜色表达，必须同时使用图标或文字。

### 4.2 字体

- 中文：`Microsoft YaHei UI`，回退 `Noto Sans CJK SC`、系统无衬线字体。
- 英文与数字：`Segoe UI`，时间数字可使用 `Segoe UI Variable`。
- 不在首版引入需要额外分发授权的商业字体。

| 样式 | 字号/行高 | 字重 | 用途 |
|---|---|---|---|
| Title L | 20/28 px | 600 | 空状态标题、重要弹窗标题 |
| Title M | 16/24 px | 600 | 面板标题、媒体标题 |
| Body | 14/22 px | 400 | 普通正文、菜单项 |
| Body Strong | 14/22 px | 600 | 当前值、主要列表项 |
| Caption | 12/18 px | 400 | 时长、状态、辅助信息 |
| Time | 12/18 px | 500 | 当前时间和总时长 |

### 4.3 间距、圆角与描边

- 基础网格：4 px。
- 常用间距：4、8、12、16、20、24、32 px。
- 小控件圆角：6 px；输入框/按钮：8 px；对话框：12 px。
- 视频画布不使用圆角，避免全屏切换和画面裁剪问题。
- 标准描边：1 px `color.border`。
- 阴影仅用于弹出菜单和对话框：`0 8 28 rgba(0,0,0,0.36)`。

### 4.4 图标

- 使用统一的 2 px 线性图标，圆角端点，默认 20 × 20 px。
- 核心播放按钮内部图标 24 × 24 px。
- 推荐图标：folder-open、link、playlist、settings、previous、play、pause、stop、next、volume、mute、speed、repeat、repeat-one、list-order、fullscreen、close、trash、more、warning、retry。
- 图标必须提供 Normal、Hover、Pressed、Disabled 状态，建议使用 SVG 便于 Qt 缩放。
- 不使用 Emoji 作为正式界面图标。

### 4.5 控件交互状态

所有可操作控件至少提供：Default、Hover、Pressed、Keyboard Focus、Disabled。Toggle 控件额外提供 Selected；异步动作额外提供 Loading。

- Hover：背景提升到 `surface.3` 或图标变为主文本色。
- Pressed：背景再加深，控件可下移 1 px，但不得产生明显跳动。
- Focus：外侧 2 px 强调色焦点环，不能只依靠系统虚线框。
- Disabled：图标和文字使用 `text.disabled`，不响应 Hover。
- Tooltip：鼠标停留 500 ms 后出现；12 px 字号，深灰背景，最大宽 280 px。

## 5. 桌面窗口规范

### 5.1 窗口尺寸

| 类型 | 尺寸 |
|---|---|
| 默认窗口 | 1280 × 800 px |
| 设计基准 | 1440 × 900 px |
| 最小窗口 | 960 × 600 px |
| 推荐最大化 | 跟随系统工作区 |

- 支持自由缩放和系统最大化/还原。
- 最小尺寸以下禁止继续缩小，避免控制栏重叠。
- 设计稿以 Windows 100% 缩放绘制，同时输出 125%、150%、200% 缩放检查页。
- 若使用自定义标题栏，必须保留 Windows 标准的最小化、最大化/还原、关闭行为及可拖拽区域；否则优先使用原生窗口框架。

### 5.2 基准主界面区域

以 1440 × 900 内容区为基准：

| 区域 | 位置与尺寸 | 说明 |
|---|---|---|
| 顶部工具栏 | x0 y0 w1440 h48 | 品牌、打开入口、列表切换 |
| 中央工作区 | x0 y48 w1440 h728 | 视频画布 + 播放列表 |
| 视频区域 | x0 y48 w1080 h728 | 随列表开关变化 |
| 播放列表 | x1080 y48 w360 h728 | 默认展开，宽度可在 300～440 px 调整 |
| 底部控制区 | x0 y776 w1440 h124 | 进度、媒体信息、播放控制、状态 |

列表关闭后，视频区域占满 1440 px 宽；底部控件整体重新分布，不保留空白占位。

## 6. 页面 01：主播放器

### 6.1 顶部工具栏（48 px）

- 左侧 16 px 边距放 24 px Logo，间隔 8 px 显示“StreamBox”，14 px/600。
- Logo 组后保留 24 px 分隔空间。
- `打开文件`：次要按钮，图标 + 文字，96 × 32 px，快捷键 Tooltip 显示 `Ctrl+O`。
- `打开 URL`：次要按钮，图标 + 文字，96 × 32 px，快捷键 Tooltip 显示 `Ctrl+U`。
- 右侧依次放：播放列表开关、更多/设置入口；图标按钮 32 × 32 px。
- 工具栏底部使用 1 px 分隔线。
- V1.0 不展示搜索框、账号头像、登录或会员按钮。

### 6.2 视频画布

#### 视频播放状态

- 背景纯黑，视频以 Contain 方式缩放并居中，保持原始宽高比。
- 禁止默认裁剪、拉伸或添加装饰边框。
- 鼠标移入时不额外覆盖整层蒙版；仅在需要显示中心操作时使用轻微透明浮层。
- 双击画布进入全屏。

#### 空状态

画布中心垂直组合，整体最大宽 480 px：

1. 64 × 64 px 播放/媒体线性图标，颜色 `text.tertiary`。
2. 16 px 间距后标题“打开媒体开始播放”，20/28 px、600。
3. 8 px 后说明“选择本地音视频文件，或粘贴 HTTP/HTTPS 媒体地址”，14/22 px、次级色。
4. 20 px 后两个按钮：主按钮“打开文件”，次按钮“打开 URL”，高度 36 px。
5. 12 px 后辅助文案“也可以将文件拖放到此处”，12/18 px。

拖拽文件进入窗口时显示 2 px 强调色内描边和半透明遮罩，中心文案改为“释放以添加到播放列表”。

#### 纯音频状态

- 背景从 `#10131A` 到 `#171C28` 的轻微垂直渐变；如果 Qt 实现不使用渐变，可统一为 `surface.1`。
- 中心显示 192 × 192 px 默认封面，圆角 12 px，内容为简化声波/音符抽象图形。
- 封面下方 20 px 显示媒体标题，最多两行，每行最多约 36 个中文字符，超出省略。
- 下一行显示“音频 · MP3/FLAC/其他格式”或来源域名，12 px 次级文本。
- 纯音频时全屏按钮置为 Disabled，并在 Tooltip 说明“仅视频可进入全屏”。

#### 正在打开/连接

- 视频区中心显示 32 px 环形加载指示器。
- 下方 12 px 显示“正在打开媒体…”或“正在连接网络媒体…”。
- 再下方提供文字按钮“取消”，不得只允许等待超时。
- 保留上一帧时应覆盖 `overlay` 蒙版，避免把旧媒体误认为新媒体。

#### 正在缓冲

- 画布中心显示 32 px Loading 和“正在缓冲 42%”。
- 若无法计算百分比，显示“正在缓冲…”。
- 下方可显示“网络连接较慢”，但不要持续闪烁。
- 底部状态条同步显示缓冲信息，中心提示在恢复播放后 200 ms 内淡出。

#### 暂停

- 保持当前视频帧。
- 暂停发生后，画面中央短暂显示 56 × 56 px 半透明圆形暂停图标，800 ms 后淡出。
- 底部主按钮切换为播放图标。

#### 错误

- 保留黑色/深灰画布，中央放 40 px 警告图标。
- 标题示例：“无法播放此媒体”。
- 说明展示具体原因，最多 2 行，例如“无法连接到服务器，请检查网络或地址。”
- 操作区按错误类型显示“重试”和“打开其他媒体”。
- 不展示 FFmpeg 原始错误堆栈；技术详情放日志或可展开详情中。

### 6.3 底部控制区（124 px）

控制区自上而下分为进度行 40 px、主控制行 56 px、状态行 28 px。

#### 进度行

- 左右边距 20 px。
- 左侧当前时间宽 52 px，右对齐；右侧总时长宽 52 px，左对齐。
- 进度条位于两者之间，高 20 px 热区，视觉轨道 4 px。
- 已播放轨道使用 `accent`；缓冲轨道使用 `#596273`；未播放轨道 `#343A46`。
- 滑块默认圆点 10 px，Hover 14 px，拖动时 16 px。
- Hover/拖动显示目标时间 Tooltip，例如 `01:24`。
- 不可 Seek 媒体禁用滑块，总时长区域显示“直播”或“未知”，进度轨道使用弱化色。

#### 主控制行

采用左、中、右三组布局：

左侧媒体信息区（可伸缩，推荐 360 px）：

- 36 × 36 px 缩略图或默认媒体图标。
- 右侧标题 14 px/600，单行省略。
- 第二行显示来源或格式，12 px 次级文本；网络 URL 仅显示域名，不显示敏感查询参数。

中央播放控制组（固定居中）：

- 上一首 36 × 36 px。
- 间隔 8 px。
- 主播放/暂停按钮 44 × 44 px，圆形，背景 `accent`，图标白色。
- 间隔 8 px。
- 下一首 36 × 36 px。
- 停止按钮 36 × 36 px，放在下一首右侧 4 px；空闲或已停止时 Disabled。

右侧工具组（推荐 360 px，靠右）：

1. 倍速文字按钮，显示当前值如 `1.0×`，56 × 32 px。
2. 播放模式图标按钮 32 × 32 px。
3. 音量图标按钮 32 × 32 px。
4. 音量滑块 88 × 20 px，视觉轨道 3 px。
5. 全屏图标按钮 32 × 32 px。

窗口收窄到 1100 px 以下时：隐藏左侧媒体格式副标题，音量滑块缩至 64 px；不得隐藏播放、上下首、进度和时间。

#### 状态行

- 左边距 20 px，使用 12 px 次级文本。
- 状态图标 14 px，后接文本：`正在播放`、`已暂停`、`正在缓冲 42%`、`正在重试（1/3）` 或错误摘要。
- 右侧可显示媒体信息摘要：`1080p · H.264 · AAC`，信息未知时整组隐藏。
- 状态行不可承载需要用户确认的唯一入口。

## 7. 页面 02：播放列表面板

### 7.1 面板结构

- 默认宽 360 px，背景 `surface.2`，左侧 1 px 分隔线。
- 顶部 52 px：标题“播放列表”，后接数量 `12`；右侧依次为“添加”和更多菜单。
- 列表内容纵向滚动，底部无独立固定区。
- 空列表显示 48 px 图标、文案“播放列表为空”和“打开文件或网络地址以添加媒体”。

### 7.2 列表项

- 标准高度 64 px，左右内边距 12 px，上下 8 px。
- 左侧 40 × 40 px 缩略图/音频图标，圆角 6 px。
- 中部标题单行省略，14 px；第二行显示时长或域名，12 px。
- 右侧 Hover 时显示 28 × 28 px 更多按钮；非 Hover 时显示时长。
- 当前播放项背景 `accent.subtle`，左侧加 3 px `accent` 指示条，标题使用 `text.primary`，可选加入 14 px 播放中波形图标。
- 选中但未播放项背景 `surface.3`，不能与当前播放状态混淆。
- 加载项显示小型 Spinner；错误项显示红色警告图标和“播放失败”。
- 双击播放；右键菜单包含“播放”“从列表中移除”“打开文件所在位置”（仅本地文件）。

### 7.3 列表操作

- 顶部“添加”菜单：`添加本地文件`、`添加网络地址`。
- 更多菜单：`清空播放列表`。
- 清空非空列表时弹出确认对话框；删除单项不弹确认，可直接执行。
- V1.0 不显示拖拽排序手柄，因为排序持久化属于后续候选需求。

## 8. 页面 03：打开网络地址对话框

### 8.1 规格

- 模态对话框，宽 520 px，高度随错误文案自适应，推荐 236 px。
- 标题：“打开网络媒体”。
- 说明：“输入可直接访问的 HTTP/HTTPS 音视频地址。”
- 输入框宽 472 px，高 40 px；占位文案 `https://example.com/media.mp4`。
- 输入框右侧内嵌清空按钮，输入为空时隐藏。
- 底部右侧按钮：`取消`（次要）、`打开`（主要），均高 36 px。
- 初始焦点落在输入框；Enter 提交，Esc 取消。

### 8.2 校验状态

- 空输入：打开按钮 Disabled。
- 格式非法：输入框使用错误描边，下方显示“请输入有效的 HTTP 或 HTTPS 地址”。
- 正在连接：输入框和打开按钮 Disabled，打开按钮内显示 Spinner +“正在打开”；取消仍可用。
- 连接失败：保留用户输入并显示具体错误，提供再次提交。
- URL 可能含敏感参数，设计稿中仅使用虚构示例，不出现真实 Token。

## 9. 页面 04：倍速菜单

- 从倍速按钮上方弹出，宽 144 px，内边距 6 px，圆角 8 px。
- 菜单项高度 36 px，按以下顺序：2.0×、1.75×、1.5×、1.25×、1.0×、0.75×、0.5×。
- 当前速度左侧显示 16 px 对勾，文字使用强调色。
- 选择后立即生效并关闭菜单，按钮文本同步更新。
- 菜单不得加入 PRD 未定义的自定义输入速度。

## 10. 页面 05：播放模式菜单

- 从模式按钮上方弹出，宽 176 px。
- 三个选项：顺序播放、列表循环、单曲循环。
- 每项含 18 px 图标、中文名称和当前项对勾。
- 主界面模式图标随选择变化，Tooltip 显示完整模式名。
- 顺序播放到列表末尾后停止，界面进入“播放结束”状态。

## 11. 页面 06：全屏视频

- 视频画布占据整个屏幕，背景纯黑，保持视频原始比例。
- 鼠标移动时显示底部浮动控制层，高约 112 px，由下至上渐变遮罩承载。
- 全屏控制只保留：进度/时间、上一首、播放暂停、下一首、倍速、音量、退出全屏。
- 播放列表默认不在全屏展开；V1.0 不需要全屏侧栏。
- 鼠标静止 3 秒后隐藏控制层和鼠标指针；暂停、缓冲或发生错误时控制层保持可见。
- 右下角图标变为“退出全屏”；Esc 和双击均可退出。
- 缓冲与错误状态沿用主视频画布中心提示。

## 12. 通用对话框与反馈

### 12.1 清空列表确认

- 宽 420 px。
- 标题：“清空播放列表？”
- 正文：“列表中的 12 个项目将被移除，此操作不会删除本地文件。”
- 按钮：“取消”和危险样式“清空”。

### 12.2 Toast

- 位置：主窗口底部控制区上方居中，不能遮挡进度条。
- 宽度 240～480 px，高度至少 40 px，显示 3 秒。
- 用于轻量反馈：“已添加 3 个媒体”“已恢复网络连接”“不支持的文件已跳过”。
- 需要用户选择或导致播放中断的错误不得仅使用 Toast。

### 12.3 长错误信息

- 主信息必须是非技术语言。
- 可提供“查看详情”展开区域，但默认收起。
- 详情可显示错误码和脱敏后的来源，不展示包含 Token 的完整 URL。

## 13. 完整状态矩阵

| 状态 | 视频区 | 播放键 | 进度 | 上/下一首 | 停止 | 关键反馈 |
|---|---|---|---|---|---|---|
| Idle 空闲 | 空状态 | Disabled | Disabled | 按列表决定 | Disabled | 引导打开媒体 |
| Opening 打开中 | Spinner + 取消 | Disabled | Disabled | Disabled | 可取消 | 正在打开/连接 |
| Buffering 缓冲 | 当前帧 + 蒙版/Spinner | 显示暂停或 Disabled | 保持当前位置 | 可用 | 可用 | 百分比或缓冲文案 |
| Playing 播放 | 视频/音频封面 | 暂停图标 | 可拖动 | 可用 | 可用 | 正在播放 |
| Paused 暂停 | 当前帧 | 播放图标 | 可拖动 | 可用 | 可用 | 已暂停 |
| Stopped 停止 | 首帧/封面 | 播放图标 | 回到 00:00 | 可用 | Disabled | 已停止 |
| Ended 结束 | 尾帧/封面 | 重播图标或播放 | 终点 | 可用 | Disabled | 播放结束 |
| Error 错误 | 错误面板 | Disabled | Disabled | 有列表时可用 | Disabled | 重试/打开其他媒体 |
| Live 直播 | 视频 | 暂停图标 | 禁止 Seek | 可用 | 可用 | 显示“直播” |

## 14. 响应式与缩放规则

### 14.1 宽度变化

- `≥ 1280 px`：播放列表默认 360 px，显示全部工具与媒体副标题。
- `1100～1279 px`：列表 320 px，音量滑块缩短，媒体副标题隐藏。
- `960～1099 px`：列表 300 px；顶栏品牌名可隐藏，只保留 Logo；底部右侧控件间距缩至 4 px。
- 用户手动关闭列表后，视频区与控制区占满窗口。
- 禁止在桌面端把底部播放控制拆成两排导致位置跳变。

### 14.2 高度变化

- 底部控制区固定 124 px，顶栏固定 48 px，视频区吸收其余高度。
- 最小高度 600 px 时，播放列表仍保持顶部标题和至少 5 个列表项可见。
- 对话框若超过工作区高度，内容区可滚动，标题和操作按钮固定。

### 14.3 DPI

- 所有尺寸视为逻辑像素（dp）。
- 图标优先 SVG；位图封面至少按 2× 输出。
- 125%、150%、200% 下文字不得裁切，时间、按钮和输入框不得错位。

## 15. 动效规范

- 常规 Hover/颜色过渡：120 ms，ease-out。
- 菜单和对话框出现：160 ms，轻微淡入，不使用明显缩放弹跳。
- 播放/暂停中心反馈：淡入 100 ms，停留 500 ms，淡出 200 ms。
- 列表当前项切换：背景过渡 160 ms。
- Loading：连续旋转，周期 900 ms，减少动画设置开启时可降为静态进度符号。
- Qt Widgets 实现困难时，优先保证状态清楚，动效可简化但不可让交互无反馈。

## 16. 文案规范

- 使用简体中文，动作按钮采用“动词 + 对象”：打开文件、打开 URL、清空列表。
- 状态用短句：正在连接、正在缓冲、已暂停、播放结束。
- 避免直接展示 `AVERROR`、`codec not found` 等底层术语。
- URL 始终大写；倍速使用乘号样式 `1.5×`，不写 `1.5x`。
- 时间不足一小时使用 `MM:SS`，达到一小时使用 `HH:MM:SS`。
- 省略规则：标题中间区域单行尾部省略；本地路径和 URL 可在 Tooltip 展示脱敏后的完整值。

## 17. 无障碍与键盘设计

- 所有图标按钮必须有可读名称和 Tooltip。
- Tab 顺序：顶部打开入口 → 播放列表 → 进度 → 播放控制 → 右侧工具。
- Space 仅在焦点不位于输入框/按钮时切换播放暂停。
- 焦点位于进度条时，Left/Right 每次调整 5 秒。
- 焦点位于音量条时，Up/Down 每次调整 5%。
- F 进入/退出全屏，Esc 退出全屏或关闭最上层弹窗。
- 错误、缓冲和播放状态应向辅助技术暴露可访问名称。

## 18. 设计稿交付清单

UI 设计工具需要输出以下 Frame/页面，禁止只输出一个静态主界面：

1. `01_Main_Empty_1440x900`：空状态，播放列表展开且为空。
2. `02_Main_Video_Playing_1440x900`：视频播放，列表含 8 项，当前项高亮。
3. `03_Main_Video_Paused_1440x900`：暂停状态。
4. `04_Main_Audio_Playing_1440x900`：纯音频封面状态，全屏禁用。
5. `05_Main_Network_Connecting_1440x900`：网络连接中，可取消。
6. `06_Main_Buffering_1440x900`：缓冲 42%。
7. `07_Main_Error_1440x900`：网络连接失败，包含重试和打开其他媒体。
8. `08_Main_Playlist_Collapsed_1440x900`：播放列表关闭。
9. `09_Dialog_OpenURL_520px`：URL 初始态、输入态、格式错误态、连接中态。
10. `10_Menu_PlaybackSpeed`：倍速菜单，1.5× 已选择。
11. `11_Menu_PlayMode`：三种模式，列表循环已选择。
12. `12_Dialog_ClearPlaylist`：清空确认。
13. `13_Fullscreen_Playing_1920x1080`：全屏控制层显示。
14. `14_Fullscreen_Buffering_1920x1080`：全屏缓冲状态。
15. `15_MinWindow_960x600`：最小窗口布局。
16. `16_Component_States`：全部按钮、输入、滑块、菜单、列表项状态。
17. `17_DPI_Check`：125%、150%、200% 缩放检查。

每个 Frame 应标注组件名称、尺寸、间距、颜色 Token、文字样式以及交互说明。组件必须建立可复用 Variant，而不是在页面中复制独立图层。

## 19. 组件库命名建议

| 组件 | Variant/属性 |
|---|---|
| `Button/Primary` | Default, Hover, Pressed, Focus, Disabled, Loading |
| `Button/Secondary` | Default, Hover, Pressed, Focus, Disabled |
| `Button/Icon` | Default, Hover, Pressed, Focus, Selected, Disabled |
| `Button/PlaybackMain` | Play, Pause, Replay × 各交互状态 |
| `Slider/Progress` | Normal, Hover, Dragging, Disabled, Live |
| `Slider/Volume` | Normal, Hover, Dragging, Disabled |
| `Input/URL` | Empty, Filled, Focus, Error, Disabled |
| `Playlist/Item` | Default, Hover, Selected, Playing, Loading, Error |
| `Feedback/Toast` | Info, Success, Warning, Error |
| `Feedback/CanvasState` | Opening, Buffering, Error, Ended |
| `Menu/Item` | Default, Hover, Selected, Disabled |
| `Dialog/Base` | Default, Error, Loading |

## 20. UI 设计工具总提示词

将下面内容连同本设计文档一起交给 AI UI 工具：

```text
请为 Windows 桌面应用“StreamBox”设计一套高保真、可开发落地的深色在线音视频播放器 UI。应用使用 Qt Widgets 实现，因此设计必须结构清晰、像素尺寸明确，避免复杂玻璃拟态和难以实现的模糊特效。

使用 1440×900 基准画布，最小窗口 960×600。主界面由 48px 顶部工具栏、中央视频画布、右侧 360px 播放列表和底部 124px 控制区组成。视频画布必须为纯黑背景、保持源视频比例、禁止裁剪拉伸。底部包括进度与时间、媒体信息、上一首、突出显示的圆形播放/暂停按钮、下一首、停止、倍速、播放模式、音量和全屏。

整体视觉应专业、现代、克制、沉浸。窗口底色 #111318，一级表面 #171A21，二级表面 #1D212A，描边 #303642，主文字 #F3F5F7，次文字 #AEB6C4，强调色 #6C8CFF，警告 #F2B84B，错误 #F06A73。字体使用 Microsoft YaHei UI / Segoe UI，基础正文 14px，图标采用统一 2px 线性 SVG 风格。

不要添加账号、会员、推荐、搜索、下载、弹幕、评论、网页嗅探或媒体平台入口。必须完整设计空状态、视频播放、视频暂停、纯音频、网络连接、缓冲、播放错误、播放结束、不可 Seek 直播、播放列表展开/收起、URL 对话框、倍速菜单、播放模式菜单、清空列表确认和全屏控制层。

请严格使用设计文档中的尺寸、Token、状态矩阵和交互规则，建立可复用组件与 Variant。输出组件命名、Auto Layout/约束、像素间距、Hover/Pressed/Focus/Disabled/Loading 状态，并提供 960×600 最小窗口及 125%/150%/200% DPI 检查稿。所有界面文案使用简体中文。
```

## 21. 设计验收标准

- 主界面所有 P0 功能均有明确入口，且没有出现范围外功能。
- 画面、控制、列表层级清晰，主播放按钮可在 1 秒内被识别。
- 播放、暂停、停止、缓冲、错误和不可 Seek 状态不会混淆。
- 1440×900、1280×800、960×600 下控件无重叠、裁切或不可操作。
- 纯音频和视频状态均成立，纯音频全屏按钮有正确禁用态。
- 列表项支持默认、Hover、选中、当前播放、加载和错误状态。
- URL 输入包含空值、非法、连接中和失败反馈。
- 颜色、字体、间距、圆角、图标和状态均来自统一组件库。
- 设计可以通过 Qt Widgets、QSS 和 SVG 图标合理实现。
- 设计标注足够开发人员在不猜测关键尺寸和状态的情况下完成界面。

---

本设计文档描述 UI/UX 与视觉交付要求；播放器线程、解码、同步、缓冲和 FFmpeg 集成细节仍以 PRD及后续技术设计文档为准。
