# 在线网络播放器 PRD

| 项目 | 内容 |
|---|---|
| 产品名称 | StreamBox（暂定） |
| 文档版本 | V1.0 |
| 文档日期 | 2026-08-15 |
| 产品形态 | Windows 桌面端，预留跨平台能力 |
| 开发工具 | `D:\Qt\Tools\QtCreator\bin\qtcreator.exe` |
| 核心技术 | Qt / C++ / FFmpeg 8.1.2 |
| FFmpeg 源码 | `D:\ffmpeg-8.1.2\ffmpeg-8.1.2` |

## 1. 产品概述

StreamBox 是一款基于 Qt 和 FFmpeg 的桌面音视频播放器。用户可以打开本地媒体文件或输入网络媒体地址进行播放，并完成播放/暂停、停止、进度跳转、倍速播放、上/下一首切换、音量控制以及视频窗口显示等操作。

首个版本聚焦稳定、清晰、可扩展的单机播放体验，不建设账号、云端曲库、内容搜索、下载、弹幕和媒体资源服务。

## 2. 产品目标

### 2.1 核心目标

1. 支持常见本地音视频文件和 HTTP/HTTPS 网络媒体流播放。
2. 提供完整的基础播放控制和播放列表能力。
3. 实现音视频同步、流畅解码、进度跳转和倍速播放。
4. 建立解封装、解码、同步、渲染相互解耦的播放器内核，为 Windows、Linux、macOS 跨平台演进预留空间。

### 2.2 成功指标

| 指标 | 目标值 |
|---|---:|
| 本地受支持媒体打开成功率 | ≥ 99% |
| 正常网络条件下可访问媒体打开成功率 | ≥ 95% |
| 本地文件首次出画/出声时间 | ≤ 1 秒 |
| 网络媒体首次播放时间 | ≤ 3 秒（不含服务器自身延迟） |
| 连续播放稳定性 | 2 小时无崩溃、无不可恢复卡死 |
| 音画同步误差 | 通常 ≤ 80 ms |
| Seek 后恢复播放时间 | 本地 ≤ 1 秒，网络 ≤ 3 秒 |
| 主界面交互响应 | ≤ 100 ms |

## 3. 用户与使用场景

### 3.1 目标用户

- 需要播放本地音乐、电影或课程视频的桌面用户。
- 拥有合法 HTTP/HTTPS 媒体直链，希望直接在线播放的用户。
- 需要通过倍速观看课程、演讲或长视频的用户。

### 3.2 典型场景

1. 用户从文件选择器一次打开一个或多个本地媒体，系统生成播放列表并开始播放。
2. 用户粘贴一个网络媒体直链，播放器缓冲后开始播放。
3. 用户拖动进度条跳转到指定位置，播放器重新建立音视频同步。
4. 用户切换为 1.5× 或 2.0× 播放，声音和画面保持同步。
5. 当前媒体播放结束后，系统按播放模式自动播放下一项。
6. 用户双击视频区域进入全屏，再次双击或按 Esc 退出全屏。

## 4. 产品范围

### 4.1 V1.0 范围（P0）

- 本地文件打开与播放。
- HTTP/HTTPS 媒体 URL 打开与播放。
- 音频与视频播放。
- 播放、暂停、停止。
- 进度展示、时间展示、拖动 Seek。
- 倍速调节。
- 音量调节与静音。
- 播放列表、上/下一首。
- 顺序播放、单曲循环、列表循环。
- 视频窗口渲染、宽高比保持、全屏播放。
- 加载、缓冲、错误和播放结束状态反馈。

### 4.2 V1.1 候选（P1）

- RTMP、RTSP、HLS 等协议/封装的专项兼容测试。
- 播放历史与上次播放位置记忆。
- 字幕轨道选择与外挂字幕。
- 音轨选择。
- 硬件解码。
- 播放列表持久化与拖拽排序。
- 最近打开列表。

### 4.3 暂不支持

- DRM 加密内容、付费平台页面地址解析或版权保护绕过。
- 网页嗅探、视频站点解析、资源搜索和资源聚合。
- 视频编辑、转码、录屏、直播推流和文件下载。
- 账号系统、收藏云同步、评论、弹幕。

## 5. 功能需求

### 5.1 媒体加载

#### FR-01 打开本地文件

- 用户可通过“打开文件”按钮选择一个或多个媒体文件。
- 支持将文件拖入主窗口；拖入多个文件时按拖入顺序加入列表。
- 打开单个文件时将其加入列表并立即播放。
- 无法识别或无法解码时不得导致程序崩溃，应显示可理解的错误信息。

#### FR-02 打开网络地址

- 提供 URL 输入对话框，支持粘贴、清空和确认。
- V1.0 必须支持 FFmpeg 可访问的 HTTP/HTTPS 媒体直链。
- 对格式非法、DNS 失败、连接超时、HTTP 错误、证书错误和不支持的媒体分别给出提示。
- 加载期间显示“正在连接”；产生缓冲时显示“正在缓冲”。
- 默认连接超时建议为 10 秒，并提供取消加载操作。
- 网络地址可以加入播放列表，上/下一首规则与本地文件一致。

### 5.2 播放控制

#### FR-03 播放、暂停、停止

- 媒体就绪后点击播放开始输出音视频。
- 播放中点击暂停，音频与视频均保持在当前时间点。
- 暂停后继续播放，不应重新从头加载。
- 停止后释放当前解码与缓冲资源，进度回到 00:00，但保留播放列表。
- 播放按钮图标随状态在“播放/暂停”之间切换。

#### FR-04 进度与时间

- 显示“当前时间 / 总时长”，格式为 `HH:MM:SS`；不足一小时可显示 `MM:SS`。
- 点播文件显示可拖动进度条。
- 拖动期间展示目标时间，释放后执行 Seek；频繁移动时不得为每个像素同步阻塞主线程。
- Seek 成功后清空旧数据包和旧帧，从目标位置附近恢复并重新同步。
- 直播流或不可 Seek 媒体禁用进度拖动，总时长显示“直播”或“未知”。

#### FR-05 倍速播放

- 提供 0.5×、0.75×、1.0×、1.25×、1.5×、1.75×、2.0× 档位。
- 默认速度为 1.0×，当前速度必须在界面可见。
- 有视频时同时调整音频和视频节奏，保持音画同步。
- 变速后音频应尽量保持原音调；若首版采用简单重采样导致变调，必须在发布说明中明确，并将保调列为上线前质量项。
- 切换媒体时沿用用户当前倍速，重启应用后默认恢复 1.0×。

#### FR-06 音量与静音

- 音量范围 0%～100%，默认 70%。
- 支持滑块调节和一键静音。
- 取消静音后恢复静音前音量。

### 5.3 播放列表

#### FR-07 列表管理

- 每一项至少显示标题；本地项优先使用文件名，网络项可使用 URL 末段或媒体元数据标题。
- 当前播放项有明显高亮。
- 支持双击列表项播放。
- 支持删除选中项和清空列表；删除当前项时按当前播放模式选择合理的下一项。
- 同一路径或 URL 默认允许重复添加，后续可提供去重设置。

#### FR-08 上一首/下一首

- 点击下一首立即切换到列表中的下一项。
- 点击上一首：当前进度大于 3 秒时优先回到当前项开头；否则切换到上一项。
- 列表边界行为受播放模式控制。
- 当前项自然播放结束时自动进入下一项或按模式循环。

#### FR-09 播放模式

- 顺序播放：播放至列表最后一项后停止。
- 列表循环：最后一项结束后回到第一项。
- 单曲循环：当前项结束后重新播放当前项。

### 5.4 视频窗口

#### FR-10 视频渲染

- 视频在主窗口的独立播放区域显示。
- 默认保持源视频宽高比，剩余区域使用黑色填充，不拉伸画面。
- 纯音频播放时显示默认封面、媒体标题或简洁占位背景。
- 调整窗口大小时视频区域自适应，主界面不得明显卡顿。
- 首版建议将 FFmpeg 解码帧转换为 Qt 可呈现的 RGB/RGBA 格式；后续可使用 OpenGL/QRhi 降低复制与 CPU 开销。

#### FR-11 全屏

- 双击视频区域或点击全屏按钮进入全屏。
- 按 Esc、再次双击或点击退出按钮离开全屏。
- 全屏时移动鼠标显示控制栏，静止 3 秒后自动隐藏；纯音频不进入视频全屏。

### 5.5 状态与错误反馈

#### FR-12 播放状态

播放器状态至少包含：空闲、正在打开、正在缓冲、播放、暂停、停止、播放结束、错误。

- 状态切换必须由播放器内核统一管理，界面不自行推断核心状态。
- 加载和缓冲状态必须有可见反馈，避免用户重复点击。
- 发生错误后用户仍可切换其他媒体，不需要重启程序。

#### FR-13 错误提示

错误提示应包含简短原因和可行动建议，例如：

- “无法连接到服务器，请检查网络或地址。”
- “媒体格式或编码暂不支持。”
- “文件不存在或没有读取权限。”
- “网络连接中断，正在重试（1/3）。”

网络读取失败可自动重试最多 3 次，建议采用 1 秒、2 秒、4 秒退避；用户主动停止或切换媒体时立即终止重试。

## 6. 界面与交互

### 6.1 主界面布局

```text
┌────────────────────────────────────────────────────────────┐
│ 菜单/工具栏：打开文件 | 打开 URL | 播放列表 | 设置          │
├──────────────────────────────────────┬─────────────────────┤
│                                      │ 播放列表            │
│             视频显示区域             │ 1. 当前媒体         │
│       （音频时显示封面/占位图）       │ 2. 下一媒体         │
│                                      │ ...                 │
├──────────────────────────────────────┴─────────────────────┤
│ 00:12  ━━━━━━━━━●━━━━━━━━━━━━━━━━━━━━  03:45             │
│ 上一首  播放/暂停  停止  下一首  倍速  音量  模式  全屏    │
│ 状态：正在播放 / 正在缓冲 42%                              │
└────────────────────────────────────────────────────────────┘
```

### 6.2 快捷键建议

| 快捷键 | 操作 |
|---|---|
| Space | 播放/暂停 |
| Ctrl+O | 打开本地文件 |
| Ctrl+U | 打开网络地址 |
| Left / Right | 后退/前进 5 秒 |
| Up / Down | 音量增加/降低 5% |
| M | 静音/取消静音 |
| N / P | 下一首/上一首 |
| F | 进入/退出全屏 |
| Esc | 退出全屏或关闭当前弹层 |

## 7. 非功能需求

### 7.1 性能

- 解封装、解码和网络读取不得运行在 Qt GUI 主线程。
- 采用有界数据包队列和帧队列，防止长时间播放导致内存持续增长。
- 以 1080p H.264 视频作为 V1.0 基准，目标为主流桌面 CPU 上稳定播放。
- 暂停、停止、Seek、切换媒体时线程应可中断并在 2 秒内完成状态切换。

### 7.2 稳定性

- 空文件、损坏文件、超大分辨率、未知时长、网络断开和服务器无响应不得导致崩溃。
- 播放器退出时有序停止读取、解码和渲染线程，避免死锁或资源泄漏。
- 连续切换媒体 100 次后程序保持可用。

### 7.3 可用性与可访问性

- 所有图标按钮提供 Tooltip。
- 禁用态、加载态、错误态视觉上可区分。
- 关键操作既支持鼠标也支持键盘。
- 中文界面为首发语言，文本通过 Qt 翻译机制管理，预留英文支持。

### 7.4 安全与合规

- 仅处理用户主动选择的本地文件和输入的网络地址。
- 不记录 URL 中的鉴权参数到普通日志；日志需要脱敏。
- 默认校验 HTTPS 证书，不提供全局忽略证书错误的发布选项。
- FFmpeg 编译配置和发布组件须核查 LGPL/GPL 及所启用第三方编解码器许可证；发布包附带所需许可证文本和源码/链接义务说明。
- 不提供对 DRM 或内容平台访问控制的绕过功能。

### 7.5 跨平台

- 业务层避免使用 Windows 专属 API。
- 文件路径统一使用 Qt 路径类型和 UTF-8/Unicode 转换。
- 平台相关音频输出、视频渲染和打包逻辑通过接口隔离。
- V1.0 在 Windows 验收；Linux、macOS 作为后续正式适配目标。

## 8. 技术方案约束

### 8.1 技术栈

- IDE：Qt Creator，路径为 `D:\Qt\Tools\QtCreator\bin\qtcreator.exe`。
- 语言：C++17 或更高。
- UI：Qt Widgets（适合首版桌面播放器；若项目确定使用 QML，可在详细设计阶段替换）。
- 构建：CMake，使用 Qt Creator Kit 管理编译器和 Qt 版本。
- 媒体：FFmpeg 8.1.2，使用 `libavformat`、`libavcodec`、`libavutil`、`libswresample`、`libswscale`，按需要使用 `libavfilter`。
- 音频输出：Qt Multimedia 的底层音频输出接口或跨平台音频设备抽象。
- 视频输出：首版 Qt 图像/OpenGL 控件，保持渲染层可替换。

### 8.2 FFmpeg 工程前置条件

`D:\ffmpeg-8.1.2\ffmpeg-8.1.2` 经检查是 FFmpeg 源码树，目前未发现可直接链接的 `.lib`、`.dll` 或完整预编译 SDK。开始播放器开发前必须完成以下工作：

1. 确认 Qt Creator 所用 Kit 的编译器（MSVC 或 MinGW）、架构（建议 x64）和运行库。
2. 使用与 Qt Kit ABI 兼容的工具链编译 FFmpeg 8.1.2，生成头文件、导入库/静态库及运行时 DLL。
3. 不得将 MSVC 构建的 FFmpeg 库与 MinGW Qt Kit 混用，反之亦然。
4. CMake 中显式配置 include、library 和运行时部署路径，禁止仅依赖开发机全局 PATH。
5. 在干净 Windows 环境验证发布目录可运行。

建议最终形成如下独立 SDK 目录，而不是直接链接源码目录：

```text
ffmpeg-sdk/
├─ include/
├─ lib/
└─ bin/
```

### 8.3 播放器内核模块

| 模块 | 主要职责 |
|---|---|
| PlayerController | 接收 UI 命令、维护统一状态机、调度各模块 |
| Demuxer | 打开输入、读取音视频包、处理网络中断与 Seek |
| AudioDecoder | 音频解码、格式转换、重采样与时钟维护 |
| VideoDecoder | 视频解码、像素格式转换和帧输出 |
| AVSync | 选择主时钟并控制视频帧展示/丢帧 |
| AudioOutput | 向系统音频设备输出 PCM |
| VideoRenderer | 在 Qt 视频窗口渲染帧 |
| PlaylistModel | 播放列表、当前项和播放模式 |
| MediaProbe | 获取时长、编码、分辨率、标题等元数据 |

音视频同步建议优先以音频时钟为主；无音频时使用视频或外部时钟。网络读取需要使用 FFmpeg 中断回调，以便停止、Seek、退出和超时时及时打断阻塞 I/O。

## 9. 数据与状态定义

### 9.1 播放列表项

| 字段 | 说明 |
|---|---|
| id | 应用内唯一标识 |
| source | 本地绝对路径或网络 URL |
| sourceType | Local / Network |
| title | 展示标题 |
| duration | 总时长，未知时为空 |
| mediaType | Audio / Video / Unknown |
| status | Ready / Loading / Error |

### 9.2 状态转换

```text
Idle → Opening → Buffering → Playing ↔ Paused
                    ↓           ↓         ↓
                  Error       Stopped   Stopped
                                ↓
                              Ended → 下一项 / Idle
```

Stop、切换媒体和退出在任何非 Idle 状态下均应可触发，并能取消正在进行的网络读取与解码操作。

## 10. 验收标准

### 10.1 核心验收用例

| 编号 | 场景 | 预期结果 |
|---|---|---|
| AC-01 | 打开 MP3/AAC/FLAC/WAV 本地音频 | 正常出声，时间与进度更新 |
| AC-02 | 打开 MP4/MKV/MOV/WebM 常见视频 | 正常出画出声，保持宽高比 |
| AC-03 | 输入有效 HTTP/HTTPS 媒体直链 | 显示连接/缓冲状态并开始播放 |
| AC-04 | 输入无效地址或断网 | 超时后提示原因，UI 不假死，可继续打开其他媒体 |
| AC-05 | 播放中暂停再继续 | 从暂停位置继续，音画保持同步 |
| AC-06 | 拖动到媒体中部 | 从目标位置附近恢复，旧画面/声音不继续输出 |
| AC-07 | 依次切换全部倍速档位 | 速度变化正确，音视频不持续失步 |
| AC-08 | 点击上/下一首 | 按产品规则切换且列表高亮同步 |
| AC-09 | 列表自然播放结束 | 按顺序/单曲循环/列表循环规则处理 |
| AC-10 | 调整窗口与切换全屏 | 画面自适应且不拉伸，Esc 可退出 |
| AC-11 | 播放纯音频 | 显示占位画面，视频相关操作合理禁用 |
| AC-12 | 播放不可 Seek 的直播流 | 进度拖动禁用并显示“直播/未知” |
| AC-13 | 播放时拔网/恢复网络 | 给出状态反馈，按策略重试或可安全失败 |
| AC-14 | 连续播放 2 小时 | 无崩溃、死锁，内存无持续无界增长 |
| AC-15 | 连续切换媒体 100 次 | 程序可用，无明显资源泄漏或残留声音 |

### 10.2 建议兼容测试集

- 音频编码：MP3、AAC、FLAC、Opus、Vorbis、PCM。
- 视频编码：H.264、H.265/HEVC、VP9、AV1（以实际 FFmpeg 编译配置为准）。
- 容器：MP4、MKV、MOV、WebM、AVI、MPEG-TS。
- 网络：HTTP、HTTPS；HLS/RTSP/RTMP 在 V1.1 前完成专项验证。
- 特殊样本：无音频视频、无视频音频、可变帧率、损坏文件、未知时长、超长文件、中文/空格路径。

## 11. 里程碑建议

| 阶段 | 交付内容 |
|---|---|
| M0 工程准备 | 确认 Qt Kit；编译并链接 FFmpeg；最小程序完成媒体探测 |
| M1 播放内核 | 本地音视频解封装、解码、音频输出、视频显示、基础同步 |
| M2 交互功能 | 播放控制、Seek、倍速、音量、全屏、播放列表和上下首 |
| M3 网络能力 | HTTP/HTTPS 打开、缓冲状态、超时、中断、重试和错误提示 |
| M4 稳定与发布 | 兼容测试、压力测试、许可证核查、依赖打包和发布验收 |

## 12. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| FFmpeg 与 Qt Kit ABI 不一致 | 无法链接或运行时崩溃 | 固定编译器、架构、运行库，先完成最小链接验证 |
| 网络源协议和服务端行为差异大 | 打开失败、卡顿、Seek 不可用 | 明确首版只保证 HTTP/HTTPS 直链；建立真实测试样本集 |
| GUI 线程承担解码或阻塞 I/O | 界面假死 | 解封装、解码独立线程，使用队列和中断回调 |
| 倍速引起变调或失步 | 体验不达标 | 采用保调滤镜/算法并进行长时音画同步测试 |
| Seek 后残留旧帧 | 短暂倒退、杂音 | Flush 解码器与队列，重建时钟并丢弃目标前帧 |
| 发布许可证配置不清 | 合规风险 | 固化 FFmpeg configure 参数并在发布前完成许可证审查 |
| 软件解码高分辨率视频占用高 | 掉帧、发热 | 首版限定性能基准，后续加入硬件解码和 GPU 渲染 |

## 13. 待项目启动时确认

以下事项不阻塞本 PRD，但必须在 M0 阶段形成技术决策记录：

1. Qt 的具体版本、Qt Widgets/QML 选择、Kit 编译器及 x64 架构。
2. FFmpeg 采用 LGPL 还是 GPL 配置，以及是否启用外部编解码器。
3. 音频输出采用 Qt Multimedia 还是独立音频库。
4. V1.0 是否将 HLS、RTSP、RTMP 纳入正式承诺范围。
5. 倍速保调采用 `libavfilter`（如 `atempo`）还是其他时伸缩方案。
6. 首版发布形式（绿色压缩包或安装程序）及最低 Windows 版本。

---

本文档定义产品需求和验收边界。线程模型、队列容量、时钟算法、CMake 目录结构和 FFmpeg 编译参数应在技术设计文档中进一步固化。
