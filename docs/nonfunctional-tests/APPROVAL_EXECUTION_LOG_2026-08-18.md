# 受阻测试批准后执行记录

日期：2026-08-18  
范围：用户批准类别1、2、3、4、6、10。未批准类别5、7、8、9未执行。

## 1. 分段累计两小时稳定性

- 首段180秒后因审批流程纠正而中断；用户批准保留CSV并从累计时间续跑。
- 第二段续跑7022秒，总累计7202秒；不能描述为严格连续2小时。
- 119个60秒样本，续跑退出码0。
- CPU：最小0.711%，最大1.361%，平均1.110%。
- 工作集：最小83.46 MB，最大128.69 MB，平均92.695 MB，首尾-36.88 MB。
- 私有字节：最小90.98 MB，最大99.96 MB，平均98.866 MB，首尾-0.01 MB。
- 线程：21–25，首尾-2；句柄：932–945，首尾-9。
- CSV：`evidence/soak_2h_2026-08-17.csv`；SHA-256 `A5FE3DD6E369BAE17B0C40B8B3E46A6C682C91E532CFAE7EB3861ED240106034`。
- 结论：未见持续泄漏、崩溃或无响应；分段属性是限制条件。

## 2. 压力媒体

- NASA 4K60官方资源下载速度过低，约39秒仅取得694 KB，已停止并删除不完整文件。
- 改用明确标为CC0且公布大小/哈希的样本：
  - `sample-4k.mp4`：72,112,734字节，SHA-256匹配；实测3840×2160、H.264/AAC、30秒；解码和运行时探针退出0，暂停2000 ms后继续至2554 ms。
  - `sample-60fps.mp4`：20,225,100字节，SHA-256匹配；实测1280×720、H.264/AAC、20秒；运行时产生150帧，暂停2000 ms后继续至2500 ms。
- 第二个样本的页面元数据与文件实测不一致，已登记风险。

## 3. 隔离网络可靠性

- 仅监听127.0.0.1，未修改系统网络配置。
- 首批解码探针：20 Mbps/50 ms、5 Mbps/150 ms、2 Mbps/300 ms、首次连接中断后恢复，均退出0。
- 真实播放器首次中断后重连退出0，暂停2017 ms后继续至2517 ms。
- 2 Mbps/300 ms真实播放器探针20秒超时，状态Playing、位置4969 ms、18帧，并有包损坏日志；可能含临时服务器Range实现影响，结论待专业工具复核。

## 4. TLS和重定向

- 临时证书CN/SAN为localhost，只监听127.0.0.1，未导入信任库，测试后删除。
- curl正确以退出60拒绝不受信根。
- 播放器对`https://localhost`和主机名不匹配的`https://127.0.0.1`均成功产生音视频并退出0。
- 结论：P0 TLS证书验证缺陷已确认。
- 重定向1级、5级退出0；20级和循环重定向约9.1秒退出2，无卡死。

## 5. Narrator/UI Automation

- 18个可聚焦元素中14个Name为空。
- 打开文件、打开URL、添加和速度按钮有名称；多数纯图标按钮、列表、画布和滑块无名称。
- Narrator启动后焦点被系统锁屏相关元素抢占，完整Tab顺序未能可靠测量。
- 本轮启动的Narrator及临时应用进程已清理。

## 6. 安全与清理

- 未关闭TLS校验、未导入证书、未访问内网或第三方非授权目标。
- 临时证书和不完整NASA下载已删除；保留获批的CSV、校验通过的媒体及测试文档。
- 未修改产品源码、构建脚本、产品配置或依赖，未实施修复。
