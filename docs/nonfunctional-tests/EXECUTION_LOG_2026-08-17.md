# 非功能测试执行记录：2026-08-17

## 执行边界

- 状态：已执行（阶段1首轮）；部分受阻。
- 未修改源码、构建脚本、配置和依赖；未安装工具、修改网络/证书/显示/音频设置或执行修复。
- Git分支保持 `main`，提交 `b610b59f74fecc110f624c2d5fe2571b299c1b94`；仅非功能测试描述文件有新增/修改。

## 环境

- Windows 11 家庭中文版64位，版本10.0.26200。
- Lenovo 82GR；AMD Ryzen 7 4800H，8核16线程；物理内存约16 GB。
- NVIDIA GTX 1050 Ti，驱动32.0.15.9597；GameViewer Virtual Display Adapter。
- NVIDIA当前输出1920×1080；AppliedDPI=120（125%）。
- 可枚举Realtek、NVIDIA及网易虚拟音频设备。本轮未切换设备。
- Qt 6.11.0 MinGW 64-bit；现有项目FFmpeg运行库。

## 已执行结果

### NF-PERF-001 启动基线（部分）

隐藏窗口启动到进程报告 `Responding`，5轮：284、37、50、37、47 ms；全部退出码0。中位数47 ms，最小37 ms，最大284 ms。该指标只是进程响应代理，不等价于“窗口完全绘制并可交互”，因此不作通过判定。

### NF-RES-001 空闲资源（部分）

15秒、1秒间隔，共15个有效样本：工作集稳定84.77 MB，私有字节38.95 MB，线程10，句柄714；退出码0。采样窗口较短，只能作为本机短时基线，不能证明无泄漏。

### NF-RES-001 本地播放资源（部分）

样本：`.tooling/sintel-trailer.mp4`；应用自然结束前取得10个约1秒间隔样本，退出码0。

| 指标 | 起始 | 峰值 | 结束（最后有效样本） | 净增长 |
|---|---:|---:|---:|---:|
| 工作集 MB | 106.12 | 106.52 | 106.51 | +0.39 |
| 私有字节 MB | 73.79 | 74.21 | 74.17 | +0.38 |
| 线程 | 29 | 29 | 28 | -1 |
| 句柄 | 942 | 942 | 942 | 0 |

CPU首个样本包含进程启动累计时间，不与稳态混用；其后样本约0.59%–0.93%（按16逻辑处理器归一化）。GPU进程计数器均为0，不能据此判定GPU没有工作。系统网卡数据混有其他进程，不能作为StreamBox进程网络流量。

### 打开到首批解码输出代理指标

- 本地媒体探针5轮：93、76、74、75、82 ms；中位数76 ms，范围74–93 ms；全部退出码0。
- W3C HTTPS媒体探针3轮：2614、2444、2533 ms；中位数2533 ms，范围2444–2614 ms；全部退出码0。

探针结束条件是媒体已打开且声明的音频/视频均产生非空解码输出，因此该时间可作“打开到首批解码输出”的代理，不能拆分网络连接、媒体就绪、首音频和首个可见视频帧。

## 未执行与受阻

- 加载、暂停、跳转、切换和停止各状态的60秒独立资源段：现有CLI/探针不能在外部可靠驱动并标记所有状态。
- 独立首帧、播放/暂停响应、跳转和切换时延：缺事件级测试遥测。
- 进程级GPU和网络流量：现有计数器未形成可信数据。
- 长稳、4K、高码率、高帧率、超大文件：缺授权标准样本，且长时高负载待审批。
- 音画同步量化：缺同步样本和外部采集设备。

## 判定

阶段1首轮状态为“已执行（部分受阻）”，不是“完成”或“通过”。已取得的数据建立了当前机器的建议基线；正式失败/通过阈值仍待确认。

## 后续可执行项目记录

### NF-STR-002 高频控制与重复加载（已执行）

- CTest 3/3通过，总耗时6.74秒：`playback_policy`、`decoder_worker`、`ffmpeg_sdk_link_and_runtime`。
- `decoderworker_test.exe`独立复跑退出码0。现有用例包含100次媒体切换、暂停/继续、跳转、停止、HTTP中断重连及重试取消。
- 结论：当前自动化覆盖内未发生崩溃、死锁或残留工作线程。由于没有事件级资源采样，本项为“已执行”，尚不能按资源增长阈值判定“完成”。

### NF-SEC-002 异常媒体健壮性（已执行：部分）

- 将现有可执行文件作为伪媒体输入：明确失败，退出码2，无崩溃/卡死。
- 0字节临时媒体：明确失败，退出码2，无崩溃/卡死；临时文件测试后已删除。
- 损坏、截断、异常元数据样本矩阵缺少授权样本，标记受阻/跳过。

### NF-COMP-001 当前Windows显示与音频（已执行：部分）

- 默认输出为耳机（Realtek(R) Audio）；48 kHz、双声道、S16，处理3.00秒，sink error 0，退出码0。
- Windows 11、1920×1080、125%缩放下，本地H.264/AAC视频成功播放并生成截图 `evidence_windows_125pct_local_video.png`。
- 人工检查截图：视频画面可见，播放列表、进度、控制栏和状态文本未见裁切或重叠。
- 未进行本轮人耳听感确认；其他声卡、DPI、分辨率和多显示器受阻/跳过。

### NF-REL-001 便携发行目录（已执行：部分）

- `dist/StreamBox`包含1个EXE、35个DLL和1个MD；核心FFmpeg DLL均存在。
- `StreamBox.exe` SHA-256：`6FF4454DD21AFF27F943119D6C051D203695BC35C9EE0170FBE7FC9420D884C0`。
- 便携程序2秒启动测试退出码0。
- 未检查数字签名、正式发布清单或干净VM；安装、升级、降级、卸载受阻/跳过。

### NF-SEC-003 敏感词静态检查（已执行：初筛）

排除构建、发行、第三方和本测试文档后，源码及既有项目文档未命中Cookie、Authorization、Bearer、access token、password或secret关键词。该结果只是静态初筛，不替代运行时日志、崩溃转储或网络抓包检查。

### NF-RES-002 30分钟长稳预检（已执行）

- 媒体：`C:\Users\19251\微灵医疗一面.mp4`，文件大小982,643,869字节；只读访问。
- 时长：1801秒采样，进程总运行1831秒；30秒采样间隔；退出码0。
- 首个播放样本：CPU 1.05%，工作集128.02 MB，私有字节99.21 MB，线程29，句柄943。
- 峰值：工作集128.02 MB；观测到的私有字节峰值100.36 MB；线程29；句柄943。
- 最终样本：CPU 1.07%，工作集90.57 MB，私有字节99.83 MB，线程21，句柄932。
- 首尾净变化：工作集-37.45 MB，私有字节+0.62 MB，线程-8，句柄-11。
- 私有字节在约92.29–100.36 MB间周期回落/恢复，工作集在初始化后主要处于约83.4–93.8 MB；句柄在932–942间波动并回落，没有观察到单向增长。
- 判定：30分钟预检未发现持续资源泄漏、崩溃、卡死或非零退出；因时长不足，不能替代2小时正式门禁。

### NF-SEC-001 URL与协议策略（已执行：部分）

只读核对确认GUI URL入口提示并仅接受合法HTTP/HTTPS scheme；其他scheme显示“请输入有效的 HTTP 或 HTTPS 地址”。播放器网络层根据HTTP/HTTPS识别网络输入。未发现显式FFmpeg `protocol_whitelist`、重定向次数上限或内网地址限制；这些属于待评估风险，未在无隔离环境下动态探测本机/内网目标。

### NF-UX-001 键盘与状态反馈（已执行：部分）

- 自动激活StreamBox窗口成功，通过空格执行暂停、再次空格继续，并发送右方向键跳转；程序退出码0。
- `evidence_keyboard_pause_resume.png`显示进度约5秒、状态“正在播放”且视频画面可见，证明继续和跳转后仍处于播放状态。
- 静态核对存在Ctrl+O、Ctrl+U、Space、F、M、N、P、方向键操作以及加载、缓冲、暂停、错误和结束反馈。
- 未发现显式`setAccessibleName`配置；完整Tab顺序、对比度量化和屏幕阅读器测试受阻/跳过。
